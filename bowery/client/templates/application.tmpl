<!doctype html>
<head>
<link rel="stylesheet" href="/styles/main.css" />
<title>{{.Application.Name}}</title>
</head>
<body>

  <div class="container login">
    <div class="nav">
      <a href="/new" class="create-app-link">+</a>
    </div>
    <form>
      <h1><input type="text" value="{{.Application.Name}}"></h1>
      <input type="text" value="{{.Application.Start}}">
      <input type="text" value="{{.Application.Build}}">
    </form>

    <div class="group application">
    </div>

    <div class="footer">
      <p>{{.Status}}</p>
      <a href="/settings" class="settings-link">&#x2699;</a>
    </div>
  </div>
</body>
